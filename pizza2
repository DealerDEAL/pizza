#include "menu.h"
#include "person.h"

using namespace std;

int main() {
    int i = 0;
    //pd(i);
    Pizzeria c;
    c.Pd(i);
    menu k;
    k.Md(i, c);
    person b;
    b.pd(i);

    
    return 0;
}
