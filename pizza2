#include <stdio.h>
#include <string.h>
#include <stdlib.h> 
#include <time.h>
#include <iostream>
#include <iomanip>
#include <fstream>
#include "menu.h"

using namespace std;

int main() {
    int i = 0;
    //pd(i);
    Pizzeria c;
    c.Pizzeria_data(i);
    menu k;
    k.md(i, c);

    
    return 0;
}
